{
  "position" : "top",
    "height" : 30,
    "modules-left" : ["custom/searchMenu", "sway/workspaces", "sway/mode", "wlr/taskbar" ],
    "modules-right" : ["tray", "custom/spotify", "memory", "pulseaudio", "battery", "battery#bat2", "network", "backlight", "clock", "custom/power"],

    "clock": {
      "format": "{:%d/%m/%Y %H:%M}" 
    },

    "memory": {
      "format": "{used:0.1f} GB/{total:0.1f} GB",
    },

    "backlight": {
      "format": " {percent}%"
    },

    "custom/power": {
      "format": "<span size=\"x-large\"></span>",
      "on-click": "wlogout"
    },

    "custom/searchMenu": {
      "format": "<span size=\"x-large\"></span>",
      "on-click": "nwggrid"
    },

    "custom/spotify": {
      "format": "{}",
      "exec": "playerctl --no-messages metadata --follow --format ' {{title}}   {{artist}}'"
    },

    "battery": {
      "interval": 10,
      "format": "{icon} {capacity}%",
      "states": {
        "warning": 25,
        "recharge": 20,
        "critical": 15
      },
      "format-icons": {
        "discharging": ["", "", "", "", "", "", "", "", "", "", ""],
        "charging": [""]
      },
    },

    "battery#bat2": {
      "interval": 10,
      "format": " {power} W"
    },

    "pulseaudio": {
      "on-click": "alacritty -e pulsemixer",
      "on-click-right": "pulsemixer --toggle-mute",
      "format": "{icon} {volume}%",
      "format-muted": "婢 Muted",
      "format-icons": {
        "default": ["奄", "奔", "墳"]
      }
    },

    "wlr/taskbar": {
      "on-click": "activate",
      "on-click-middle": "close",
      "on-click-right": "maximize"
    },

    "network": {
    "interface": "wlan0",
    "format": "{ifname}",
    "format-wifi": "{essid} ({signalStrength}%) ",
    "format-disconnected": "", //An empty format will hide the module.
    "tooltip-format": "{ifname} via {gwaddr} ",
    "tooltip-format-wifi": "{essid} ({signalStrength}%) ",
    "tooltip-format-ethernet": "{ifname} ",
    "tooltip-format-disconnected": "Disconnected",
    "max-length": 50,
    "on-click": "alacritty -e nmtui"
    }
}

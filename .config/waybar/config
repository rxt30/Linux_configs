{
  "position" : "top",
    "height" : 30,
    "modules-left" : ["custom/searchMenu", "sway/workspaces", "sway/mode", "wlr/taskbar" ],
    "modules-right" : ["tray", "custom/spotify", "memory", "pulseaudio", "battery", "battery#bat2", "backlight", "clock", "custom/power"],

    "clock": {
      "format": "{:%d/%m/%Y %H:%M}" 
    },

    "memory": {
      "format": "{used:0.1f} GB/{total:0.1f} GB",
    },

    "backlight": {
      "format": " {percent}%"
    },

    "custom/power": {
      "format": "<span size=\"x-large\"></span>",
      "on-click": "nwgbar -o 0.5"
    },

    "custom/searchMenu": {
      "format": "<span size=\"x-large\"></span>",
      "on-click": "nwggrid"
    },

    "custom/spotify": {
      "format": "{}",
      "exec": "playerctl --no-messages metadata --follow --format ' {{title}}   {{artist}}'"
    },

    "battery": {
      "interval": 10,
      "format": "{icon} {capacity}%",
      "states": {
        "warning": 25,
        "recharge": 20,
        "critical": 15
      },
      "format-icons": {
        "discharging": ["", "", "", "", "", "", "", "", "", "", ""],
        "charging": [""]
      },
    },

    "battery#bat2": {
      "interval": 10,
      "format": " {power} W"
    },

    "pulseaudio": {
      "on-click": "kitty pulsemixer",
      "on-click-right": "pulsemixer --toggle-mute",
      "format": "{icon} {volume}%",
      "format-muted": "婢 Muted",
      "format-icons": {
        "default": ["奄", "奔", "墳"]
      }
    }
}
